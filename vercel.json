{
  "buildCommand": "cd packages/shared && npm run build && cd ../../apps/web && npm run build",
  "outputDirectory": "apps/web/dist",
  "installCommand": "npm ci",
  "rewrites": [
    { "source": "/api/:path*", "destination": "https://YOUR-APP.onrender.com/api/:path*" }
  ]
}
